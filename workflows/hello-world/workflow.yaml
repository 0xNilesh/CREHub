# ==========================================================================
# CRE WORKFLOW SETTINGS — hello-world
# ==========================================================================

# --------------------------------------------------------------------------
local-simulation:
  user-workflow:
    workflow-name: "crehub-hello-world-local"
  workflow-artifacts:
    workflow-path: "./src/index.ts"
    config-path: "./config.json"

# --------------------------------------------------------------------------
staging-settings:
  user-workflow:
    workflow-name: "crehub-hello-world-staging"
  workflow-artifacts:
    workflow-path: "./src/index.ts"
    config-path: "./config.json"

# --------------------------------------------------------------------------
production-settings:
  user-workflow:
    workflow-name: "crehub-hello-world-production"
  workflow-artifacts:
    workflow-path: "./src/index.ts"
    config-path: "./config.json"
